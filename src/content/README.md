Blog content lives in `src/content/blog/*.json`.
Add one JSON file per article with fields like `slug`, `title`, `summary`, `category`, `points`, and `sections`.

Academy hub content lives in `src/content/academy/academy.json`.
Update that file to add or change audiences, tracks, resources, and outcomes without touching page JSX.
